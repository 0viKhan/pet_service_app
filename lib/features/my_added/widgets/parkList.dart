import 'package:flutter/cupertino.dart';

class ParkList extends StatelessWidget {
  const ParkList({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("No Park Added"),
    );
  }
}